import PropTypes from "prop-types";

const WishBook = ({ book }) => {
    return (
        <div>

        </div>
    );
};

WishBook.propTypes = {
    book: PropTypes.object.isRequired
}

export default WishBook;